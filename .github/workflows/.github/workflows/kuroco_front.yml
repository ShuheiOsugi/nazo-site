name: Deploy static site to KurocoFront
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy static HTML (no build)
        run: echo "Static HTML deployment - no build step"
