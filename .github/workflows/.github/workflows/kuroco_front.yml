name: Deploy static HTML to KurocoFront
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Prepare static files
        run: echo "Static HTML deployment - no build step needed"
