name: Deploy static site to KurocoFront
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Archive artifact for KurocoFront
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: ./
