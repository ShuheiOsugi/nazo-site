{
  "name": "nazo-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'No dev server needed'",
    "build": "mkdir -p out && (cp *.html out/ 2>/dev/null || true) && (cp style.css out/ 2>/dev/null || true)",
    "start": "echo 'No start needed'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
